pipeline{
    agent any
    stages{
       stage('Test') {
        steps {        
            sh 'cat test.robot'
            sh 'robot -d Result test.robot'
        }
    }
}
    post {
        always {
    // One or more steps need to be included within each condition's block.
             robot 'Result'
         }
    }
}
