pipeline {
    agent any
    stages {
        stage('Test') {
            parallel "No1": {
                sh 'cat test.robot'
                sh 'robot -d Result test.robot'
            },
            "No2": {
                sh 'cat test.robot'
                sh 'robot -d Result test.robot'
            }
        }
    }
    post {
        always {
            robot 'Result'
        }
    }
}
